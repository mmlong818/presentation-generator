# 演讲生成器 · 环境变量
#
# 注意：本项目默认无需任何环境变量。
# 用户的 LLM API key 通过浏览器界面输入，存在 localStorage，不上传服务器。
#
# 以下所有变量都是可选的。

# ─── 可选：默认 LLM key（部署版给访客免登录体验时用）────────────────────────
# 如果你想让访客无需自带 key 即可生成（你掏钱），可以取消下面任意一行的注释。
# 强烈建议同时实现速率限制（防被刷爆）。
# DEFAULT_ANTHROPIC_API_KEY=
# DEFAULT_OPENAI_API_KEY=

# ─── 可选：生产环境标识 ──────────────────────────────────────────────────────
# Vercel 自动注入 VERCEL=1，本地开发时不需要。
# claude-cli 模式仅当 VERCEL/NETLIFY 都未设置时启用。

# ─── 可选：数据库（如果你接了 SQLite/Postgres）──────────────────────────────
# 详见 docs/DATABASE.md
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# SQLITE_PATH=./.data/decks.db
